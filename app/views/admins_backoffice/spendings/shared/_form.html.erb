<!-- Basic Card Example -->
<div class="card shadow mb-4">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-primary"><%= "#{action_message} Gasto" %></h6>
    </div>
    <div class="card-body">
        <%= form_with(model: [:admins_backoffice, @spending], local: true) do |form| %>
            <div class="form-group">
            <%= form.label :date %>
            <%= form.date_field :date ,class:"form-control form-control-user" %>
            </div>

            <div class="form-group">
            <%= form.label :description %>
            <%= form.text_field :description ,class:"form-control form-control-user", placeholder:"Gasto" %>
            </div>

            <div class="form-group">
            <%= form.label :value %>
            <%= form.text_field :value ,class:"form-control form-control-user", placeholder:"Valor" %>
            </div>

            <button type="submit" class="btn btn-primary btn-user btn-block">Salvar</button>
        <% end %>
    </div>
</div>